import styled from 'styled-components'
import { YELLOW } from '../../styles/colors'

export const Title = styled.p`
    font-weight: bold;
    font-size: 28px;
    text-align: center;
    color: ${ YELLOW };
`
